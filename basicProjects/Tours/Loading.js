import React from 'react';
import ReactDOM from 'react-dom';

const Loading = () =>{
    return (
        <div className="loading">
            <h1>Loading...</h1>
        </div>
    );
}

export default Loading;